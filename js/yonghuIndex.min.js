		//需要参数  localhost() 
//	var preUrl="www.baidu.com?id=8&type=8989&identity=adfsdfdf";//window.location.href
	var preUrl=window.location.href;//window.location.href
	var splitUrl=getUrl(preUrl);
	console.log(splitUrl)
	var identity=splitUrl.identity
	var ddd=splitUrl.type;
$(".tab2 li:nth-of-type(1)").on("touchstart", function() {
	$(".huiDa").css({
		"display": "none"
	});
	$(".tieZi").css({
		"display": "none"
	});
	$(".tiWen").css({
		"display": "block"
	});
	$(".tab2 li").attr("class", "");
	$(this).attr("class", "tabActive")
});
$(".tab2 li:nth-of-type(2)").on("touchstart", function() {
	$(".tiWen").css({
		"display": "none"
	});
	$(".tieZi").css({
		"display": "none"
	});
	$(".huiDa").css({
		"display": "block"
	});
	$(".tab2 li").attr("class", "");
	$(this).attr("class", "tabActive")
});
$(".tab2 li:nth-of-type(3)").on("touchstart", function() {
	$(".tiWen").css({
		"display": "none"
	});
	$(".huiDa").css({
		"display": "none"
	});
	$(".tieZi").css({
		"display": "block"
	});
	$(".tab2 li").attr("class", "");
	$(this).attr("class", "tabActive")
});
//var urls=需要跳转到的页面
$(".header-left").on("touchstart",function() {
		if (ddd==0){
			var urls = "yishengjiaoyu/html/qzindex.html";
			getPath(urls);
		} else if (ddd==1){
			window.history.go(-1); 
		}
		
});
//点赞 点赞  点赞  点赞  点赞  点赞  点赞 点赞   点赞   点赞  点赞

//点赞 点赞  点赞  点赞  点赞  点赞  点赞 点赞   点赞   点赞  点赞


$(function() {

	$.ajax({
		url: localhost()+"user/get_user_details?identity="+identity,
		dataType: "json",
		async: true,
		type: "GET",
		beforeSend: function() {
			console.log("======== 请求之前调用 =========")
		},
		success: function(shuju) {
			console.log("========= 服务器响应成功调用 ==========");
			console.log(shuju)
			var userSpecial = shuju.data.userDetails;
			$(".photo-top-img img").attr("src", userSpecial.userUrl);
			$(".tanChaung section:nth-of-type(1)>img").attr("src", userSpecial.userUrl);
			$(".tanChaung section:nth-of-type(2) img").attr("src", userSpecial.userQrCode);
			console.log(userSpecial.userName);
			console.log(shuju);
			$(".tanChaung section:nth-of-type(1) span:nth-of-type(1)").html(userSpecial.userName);
			$(".photo-name").html(userSpecial.userName);
			if(userSpecial.userSex) {
				var img1 = document.createElement("img");
				img1.src = "./img/ic_man.png";
				$(".photo-sex").append(img1);
				$(".tanChaung-sex").attr("src", "./img/ic_man.png")
			} else {
				var img1 = document.createElement("img");
				img1.src = "./img/sex-w.png";
				$(".photo-sex").append(img1);
				$(".tanChaung-sex").attr("src", "./img/sex-w.png")
			}
			if(userSpecial.teacherType == "") {
				$(".ptoto-zhiwei").html(userSpecial.userType);
				console.log(userSpecial.userType);
				console.log(userSpecial.teacherType)
			} else if(userSpecial.userType == "") {
				$(".ptoto-zhiwei").html(userSpecial.teacherType)
			}
			if(userSpecial.teacherType == "" && userSpecial.userType == "") {
				$(".ptoto-zhiwei").css({
					"border": "none"
				})
			}
			$(".photo-erweima img").on("touchstart", function() {
				$(".tanChaung").css({
					"display": "block"
				});
				$(".zhezhaoceng").css({
					"display": "block"
				});
				$(".tanChaung section:nth-of-type(1) span:nth-of-type(2)")
			});
			$(".zhezhaoceng").on("touchstart", function() {
				$(".tanChaung").css({
					"display": "none"
				});
				$(".zhezhaoceng").css({
					"display": "none"
				})
			});
			$(".photo-juli").html(userSpecial.distance+"米")
			$(".tab li:nth-of-type(1) span:nth-of-type(1)").html(userSpecial.problemTotal)
			$(".tab li:nth-of-type(2) span:nth-of-type(1)").html(userSpecial.answerTotal)
			$(".tab li:nth-of-type(3) span:nth-of-type(1)").html(userSpecial.collectionTotal)
			$("#btn").on("touchstart",function(){
				var urls="yishengjiaoyu/messageHTML/message.html";
				getPath(urls);
			})
			
			var socialCircle = shuju.data.socialCircleTopicUserModelList;
			for(var pinglun of socialCircle) {
				var date = new Date(pinglun.time);
				Y = date.getFullYear() + '-';
				M = (date.getMonth() + 1 < 10 ? '0' + (date.getMonth() + 1) : date.getMonth() + 1) + '-';
				D = date.getDate() + ' ';
				h = date.getHours() + ':';
				m = date.getMinutes() + ':';
				s = date.getSeconds();
				$("<li><section><img src=" + pinglun.user.userUrl + "><div><p>" + pinglun.user.userName + "</p><p>" + Y + M + D + h + m + s + "</p></div></section><section><p>" + pinglun.socialCircleTopicTitle + "</p><div>" + pinglun.socialCircleTopicTitle + "</div></section><section><span><img src='img/zan.gif'>" + pinglun.likeTotal + "</span><span><img src='img/ic_answer.png'>" + pinglun.commentTotal + "</span></section></li>").appendTo(".tieZi")
			}
			for(var value of shuju.data.userDetailsProblemList) {
				var date = new Date(value.time);
				Y = date.getFullYear() + '-';
				M = (date.getMonth() + 1 < 10 ? '0' + (date.getMonth() + 1) : date.getMonth() + 1) + '-';
				D = date.getDate() + ' ';
				h = date.getHours() + ':';
				m = date.getMinutes() + ':';
				s = date.getSeconds();
				$("<li><section><p>" + value.problemTitle + "</p></section><section><span class=''>有" + value.answerTotal + "人回答</span><span class=''>" + Y + M + D + h + m + s + "</span></section></li>").appendTo(".tiWen")
			}
			console.log(shuju.data.userDetailsAnswerList);
			for(var value of shuju.data.userDetailsAnswerList) {
				console.log(value);
				var date = new Date(value.time);
				Y = date.getFullYear() + '-';
				M = (date.getMonth() + 1 < 10 ? '0' + (date.getMonth() + 1) : date.getMonth() + 1) + '-';
				D = date.getDate() + ' ';
				h = date.getHours() + ':';
				m = date.getMinutes() + ':';
				s = date.getSeconds();
				$("<li><section>" + value.problemTitle + "</section><section>" + value.acceptionContent + "</section><section>" + Y + M + D + h + m + s + "</section></li>").appendTo(".huiDa")
			}
		},
		error: function() {
			console.log("======= 服务器响应失败调用 ==========")
		},
		complete: function() {
			console.log("======== 请求完毕之后调用，注意：无论服务器响应成功与否，都会调用这个函数！！ ===========")
		}
	})
});