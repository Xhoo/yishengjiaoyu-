$(function() {
//需要参数  localhost() 
var preUrl=window.location.href;//window.location.href
var splitUrl=getUrl(preUrl);
console.log(splitUrl)
var id = splitUrl.id;	
var sousuo=$(".header-center input").val()
	$.ajax({
		url: localhost()+"problem/get_search_for_list?type=0&text="+sousuo,
		dataType: "json",
		async: true,
		type: "GET",
		beforeSend: function() {
			console.log("======== 请求之前调用 =========")
		},
		success: function(shuju) {
		if(shuju.code==1002){window.location.href=locationHref()+'yishengjiaoyu/login';}
		
			console.log("========= 服务器响应成功调用 ==========");
		},
		error: function() {
			console.log("======= 服务器响应失败调用 ==========")
		},
		complete: function() {
			console.log("======== 请求完毕之后调用，注意：无论服务器响应成功与否，都会调用这个函数！！ ===========")
		}
	})
});
$(".header-right img").on("touchstart", function() {
	var text = $(".header-center input").val();
	var url = localhost()+"problem/get_search_for_list?type=0&text=" + text;
	$.get(url, function() {
		location.href = "searchList.html?type=0&text=" + text
	})
});
$(".wall section:nth-of-type(1) span").on("touchstart", function() {
	var labelSecond = $(this).html();
	console.log(labelSecond);
	var url = localhost()+"problem/get_search_for_list?type=1&labelSecond=" + labelSecond;
	$.get(url, function(data) {
		location.href = 'searchList.html?type=1&text=' + labelSecond
	})
});
$(".wall section:nth-of-type(2) span").on("touchstart", function() {
	var labelSecond = $(this).html();
	console.log(labelSecond);
	var url = localhost()+"problem/get_search_for_list?type=1&labelSecond=" + labelSecond;
	$.get(url, function(data) {
		location.href = 'searchList.html?type=1&text=' + labelSecond
	})
});
	//var urls=需要跳转到的页面
$(".header-left").on("touchstart",function(){
	var urls="yishengjiaoyu/html/qzindex.html";
	getPath(urls);
});
//http://47.90.85.71/yishengjiaoyu/zuoye.html

$(".header-right").on("touchstart",function(){
	var text=$(".header-center input").val()
	var urls="yishengjiaoyu/searchList.html?text="+text;
	getPath(urls);
});